<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between h-16">
      <div class="flex">
        <!-- Logo/Home Link -->
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "flex items-center" do %>
            <span class="text-2xl font-bold text-blue-600">EVS</span>
            <span class="ml-2 text-gray-600">Electric Vehicle Search</span>
          <% end %>
        </div>

        <!-- Navigation Links -->
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to "Home", root_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(root_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
          <%= link_to "Compare Cars", compare_cars_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(compare_cars_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
        </div>
      </div>
    </div>
  </div>
</nav> 